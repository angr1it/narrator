📤 **Формат ответа**:
- Ответ — **JSON-массив** объектов. Всегда массив, даже если найден только один объект.
- Никакого текста или объяснений вне JSON.
- Строго следуй схеме:
{{ format_instructions }}

🔎 **Важно**: при заполнении любых слотов извлекай имена собственные, объекты и другие сущности **в начальной (лемматизированной, именительной) форме**.
Например, из фразы «Маша посмотрела на Максима» нужно вернуть значение `Максим`, а не `Максима`, независимо от падежей в исходном тексте.
