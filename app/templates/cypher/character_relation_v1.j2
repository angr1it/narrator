MERGE (a:Character {name:'{{ character_a }}'})
MERGE (b:Character {name:'{{ character_b }}'})
WITH a, b
CALL apoc.create.relationship(a, '{{ relation_type|upper }}_OF', {}, b) YIELD rel

{% include 'universal_fact.j2' %}
